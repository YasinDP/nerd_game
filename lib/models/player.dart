import 'package:flutter/material.dart';

class Player {
  final int id;
  final Color color;
  final bool isActive;

  Player({
    required this.id,
    required this.color,
    required this.isActive,
  });
}
